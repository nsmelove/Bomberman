/**
* name
*/
var utils;
(function (utils) {
    var util = (function () {
        function util() {
        }
        return util;
    }());
    //弧度角度比
    util.RADIAN_PER_ANGLE = Math.PI / 180;
    utils.util = util;
})(utils || (utils = {}));
//# sourceMappingURL=util.js.map